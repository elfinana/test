'use client';

import Button from '@/shared/components/buttons/Button';

export default function TestPage() {
  return (
    <div className="w-full">
      <Button text="회원가입" disabled />
    </div>
  );
}
